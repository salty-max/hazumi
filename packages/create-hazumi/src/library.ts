/**
 * npm range the scaffold pins for `hazumi` and `@hazumi/vite-plugin`.
 *
 * Independent of this CLI's version — `create-hazumi` is not in the runtime
 * lockstep group. Bump when the generated templates need a newer API.
 */
export const HAZUMI_RANGE: string = "^0.3.0";
