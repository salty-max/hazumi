/**
 * L1 math, re-exported whole: vectors, matrices, easing, collision queries,
 * grid pathfinding and the rigid-body world.
 *
 * The operation names collide by design, so the vector and matrix modules are
 * namespaced: `vec2.add`, `vec3.add`, `mat4.multiply`.
 */
export * from "@hazumi/math";
