/**
 * Reference site entry point.
 *
 * Phase 6: TSDoc extraction from the packages, rendered as the API reference.
 * A generated reference is most of what makes a drawing API learnable, so this
 * is scheduled before the API broadens, not after.
 */

export const PLACEHOLDER: true = true;
