{
  "name": "@matter/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts && bunx @tailwindcss/cli -i src/style.css -o dist/style.css --minify",
    "dev": "bun run src/build.ts && bunx @tailwindcss/cli -i src/style.css -o dist/style.css --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "devDependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@lezer/highlight": "^1.2.3",
    "@lezer/javascript": "^1.5.4",
    "@tailwindcss/cli": "^4.3.3",
    "@types/bun": "^1.4.0",
    "matter": "workspace:*",
    "tailwindcss": "^4.3.3"
  }
}
