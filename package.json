{
  "name": "hazumi-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "bun run build && bun run --filter @hazumi/web dev",
    "serve": "bun run --filter @hazumi/web dev",
    "test": "turbo run test",
    "test:browser": "turbo run test:browser",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "ci": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run build",
    "changeset": "changeset",
    "release": "turbo run build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@commitlint/types": "^21.2.0",
    "@hazumi/backend-canvas2d": "workspace:*",
    "@hazumi/backend-headless": "workspace:*",
    "@hazumi/backend-svg": "workspace:*",
    "@hazumi/backend-webgl2": "workspace:*",
    "@hazumi/graphics": "workspace:*",
    "@types/bun": "^1.3.14",
    "hazumi": "workspace:*",
    "husky": "^9.1.7",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "tsdown": "^0.22.14",
    "turbo": "^2.10.11",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "packageManager": "bun@1.3.14"
}
